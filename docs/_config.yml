# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: DSAG ABAP Guide (Unofficial English Translation)
lang: en
email: info@dsag.de
description: >- # this means to ignore newlines until "baseurl:"
  Unofficial English translation of DSAG's ABAP-Leitfaden
baseurl: "/DSAG-ABAP-Guide" # the subpath of your site, e.g. /blog
url: "https://marianfoo.github.io" # the base hostname & protocol for your site
twitter_username: _dsag
github_username:  DSAGadmin
markdown: kramdown
color_scheme: dsag
logo: "/assets/images/DSAGLogo.svg"
search_placeholder_text: "Search DSAG ABAP Guide"

# Build settings
remote_theme: pmarsceill/just-the-docs # for github pages
# theme: "just-the-docs" # for local install
plugins:
  - jekyll-feed
  - jemoji
  - jekyll-remote-theme

callouts:
  recommendation:
    title: "Recommendation"
    color: yellow
  note:
    title: "Note"
    color: purple
  solution:
    title: "Solution"
    color: green
  warning:
    title: "Warning"
    color: red
  important:
    title: "Important"
    color: blue
  new:
    title: "New"
    color: green

mermaid:
  version: "11.3.0"

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
